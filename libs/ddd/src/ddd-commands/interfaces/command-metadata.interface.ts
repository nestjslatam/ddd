/**
 * Represents the metadata for a domain command.
 */
export interface IDomainCommandMetadata {
  /**
   * The ID of the command.
   */
  id?: string;

  /**
   * The tracking ID of the command.
   */
  trackingId?: string;
}
