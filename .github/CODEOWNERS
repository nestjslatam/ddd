# Every change needs an approving review from the owner.
#
# The account matters: GitHub SILENTLY IGNORES a CODEOWNERS entry naming a user
# without write access. This file previously named @beyondnet, which holds only
# read, so "require review from Code Owners" was configured and enforcing
# nothing. Verify with:
#
#   gh api repos/<owner>/<repo>/collaborators/<user>/permission
#
* @beyondnetPeru

# Called out separately so a change here is never lost in a large diff: these
# are the paths that decide what runs in CI and what reaches npm.
/.github/       @beyondnetPeru
/scripts/       @beyondnetPeru
package.json    @beyondnetPeru
package-lock.json @beyondnetPeru
